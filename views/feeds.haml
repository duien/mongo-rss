%h1 Feeds
%p All of the feeds there is

%ul
  - @feeds.each do |feed|
    %li
      = feed.title
      %a{ :href => "/feeds/#{feed.id}/items" }= "(#{feed.items.length} items)"
      %br
      %code&= feed.url
